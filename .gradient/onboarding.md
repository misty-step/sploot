# Gradient Onboarding

Workspace: sploot
Profile: solo-frontier

Run:

```sh
gradient resolve
gradient validate
GRADIENT_SKIP_WORKSPACE_REGRESSIONS=1 gradient eval
gradient work list --status ready
```
