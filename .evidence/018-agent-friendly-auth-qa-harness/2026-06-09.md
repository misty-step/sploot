# Agent-Friendly Auth And QA Harness Evidence

Date: 2026-06-09
Branch: `feat/018-agent-friendly-auth-qa-harness`
Backlog: `backlog.d/018-agent-friendly-auth-and-qa-harness.md`

## Acceptance Evidence

- Provider decision spike completed in `docs/auth-agent-readiness-decision-2026-06-09.md`.
- Typed auth boundary added in `apps/web/lib/auth/types.ts`, `request-auth.ts`, and `with-authenticated-api.ts`.
- Representative protected route migrated: `apps/web/app/api/cache/stats/route.ts`.
- Deterministic QA-local auth mode added in `apps/web/lib/auth/qa-local.ts`; production mode returns forbidden and does not authenticate QA tokens.
- `/app` authenticated smoke added in `apps/web/e2e/auth.spec.ts` with no Clerk storage state or real Clerk credentials.
- Extension API token-provider injection added in `apps/extension/shared/api-client.ts`.
- Docs updated in `apps/web/docs/AUTH.md`, `apps/web/docs/API.md`, and `apps/web/SETUP.md`.
- Auth boundary guard added in `apps/web/scripts/check-auth-boundary.mjs` and enforced by `apps/web` lint.

## Verification

- `DATABASE_URL='postgresql://test:test@localhost:5432/sploot_test?sslmode=disable' pnpm --filter web db:migrate`
  - Applied `20260609_add_user_identities`.
- `DATABASE_URL='postgresql://test:test@localhost:5432/sploot_test?sslmode=disable' pnpm --filter web exec vitest run __tests__/unit/sync-user-migration.test.ts __tests__/integration/user-sync.test.ts __tests__/hooks/use-sort-preferences.test.ts __tests__/lib/auth/qa-local.test.ts __tests__/lib/auth/with-authenticated-api.test.ts __tests__/middleware.test.ts __tests__/api/cache-stats.test.ts`
  - 7 files passed, 50 tests passed.
- `DATABASE_URL='postgresql://test:test@localhost:5432/sploot_test?sslmode=disable' pnpm --filter web test -- --run`
  - 70 files passed, 901 tests passed.
- `DATABASE_URL='postgresql://test:test@localhost:5432/sploot_test?sslmode=disable' pnpm lint && DATABASE_URL='postgresql://test:test@localhost:5432/sploot_test?sslmode=disable' pnpm type-check && DATABASE_URL='postgresql://test:test@localhost:5432/sploot_test?sslmode=disable' pnpm --filter web test -- --run && pnpm --filter extension build`
  - Passed; web lint included `pnpm auth:guard`.
- `pnpm --filter extension test`
  - 6 files passed, 22 tests passed.
- `DATABASE_URL='postgresql://test:test@localhost:5432/sploot_test?sslmode=disable' pnpm --filter web e2e:auth`
  - 1 Chromium test passed against `/app`.
- `DATABASE_URL='postgresql://test:test@localhost:5432/sploot_test?sslmode=disable' pnpm type-check`
  - Passed after the final Playwright timeout/config edit.

## Provider Receipts

- `codex` builder-plan lane: `5fc0911f-16b8-420d-b38b-141d338117cd`, accepted.
- `claude` fresh diff critic lane: `091f9cd9-af96-471d-a067-8a30638e9937`, accepted after enforcing `auth:guard` in lint and fixing the QA-forbidden Clerk fallthrough.
- `pi` security critic lane: `de70786f-03e2-4d2e-a94e-5c8b039da9e0`, rejected as a provider failure because the dispatch exited with `auth_required`; useful observations were independently verified before acceptance.

## Residual Risk

- QA-local is intentionally scoped to local/CI-style use and remains disabled when `NODE_ENV=production` or `VERCEL_ENV=production`.
- Only one representative route was migrated to the wrapper per ticket scope; the static guard prevents newly migrated API routes from reintroducing direct Clerk imports outside the allowlist.
- DB-backed verification used local pgvector Postgres at `postgresql://test:test@localhost:5432/sploot_test?sslmode=disable`; deployed DB health and release surfaces were not changed.
