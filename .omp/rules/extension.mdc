---
description: WXT extension environment and Chrome Web Store release contract
globs:
  - "apps/extension/**"
alwaysApply: false
---

# Chrome extension

- Clerk keys must match the target: `pk_test_*` with localhost for dev;
  `pk_live_*` with `https://www.sploot.app` for production. Dev/prod values live
  in `.env` / `.env.production` as `VITE_CLERK_PUBLISHABLE_KEY`,
  `VITE_API_BASE_URL`, and `VITE_CLERK_SYNC_HOST`.
- Build with WXT: `build`, `build:prod` or `build:prod:unpacked`, then `zip:prod`
  for a release. The release packet is gated by
  `pnpm --filter extension release:check` and includes `STORE_LISTING.md` plus
  `store-assets/`.
- Web deploy and extension release stay separate. Shared upload, MIME, and API
  logic comes from `@sploot/common`; do not fork it here.
