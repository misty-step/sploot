# Optional default instance. The popup can select a different origin.
# Defaults to the self-contained Go app; remote instances require HTTPS.
VITE_API_BASE_URL=http://127.0.0.1:3001

# Optional stable identity for production-like unpacked QA, generated locally:
# pnpm --filter extension generate:crx-key
# CRX_PUBLIC_KEY=
